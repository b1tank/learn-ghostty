{
  "name": "learn-ghostty",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:ai": "node scripts/generate-ai-content.mjs",
    "dev": "npm run generate:ai && vitepress dev site --host 127.0.0.1 --port 4173",
    "build": "npm run generate:ai && vitepress build site",
    "preview": "vitepress preview site --host 127.0.0.1 --port 4173",
    "check": "node scripts/check-course.mjs",
    "audit:ui": "node scripts/audit-ui.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.4.0",
    "vitepress": "latest",
    "vue": "latest"
  }
}

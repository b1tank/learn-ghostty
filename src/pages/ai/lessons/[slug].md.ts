import type { APIRoute } from "astro";import {getCollection} from "astro:content";import {readFile} from "node:fs/promises";import {resolve} from "node:path";import {cleanLessonMarkdown} from "../../../lib/cleanMarkdown.js";const commit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699";export async function getStaticPaths(){const entries=await getCollection("lessons",({data})=>data.status==="published");return entries.map(entry=>({params:{slug:entry.id},props:{entry}}))}export const GET:APIRoute=async({props})=>{const entry=props.entry;const raw=await readFile(resolve(`src/content/lessons/${entry.id}.mdx`),"utf8");return new Response(cleanLessonMarkdown(raw,{id:entry.id,...entry.data},commit),{headers:{"Content-Type":"text/markdown; charset=utf-8"}})};

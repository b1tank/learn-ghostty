---
title: "Chapter 10: First OpenGL rectangle"
description: Give runtime Surface a GtkGLArea, verify an OpenGL 4.3+ context, and draw one regionally verified rectangle.
order: 10
module: Reconstruction
status: published
duration: 45–60 minutes
sidebar:
  order: 10
next: false
historyRefs:
  - commit: 7f34afa3953289d33f95150c5f7d6a6d458a6993
    date: "2023-02-23"
    path: src/apprt/gtk.zig
    line: 180
    end: 238
    label: First GTK GLArea color
    purpose: See Ghostty connect realize/render callbacks, load OpenGL, and clear a GTK GLArea to one known color.
    context: Ghostty established the graphics callback before its mature renderer architecture existed.
sourceRefs:
  - path: src/renderer/OpenGL.zig
    line: 130
    end: 180
    label: Current OpenGL capability initialization
    purpose: Read version validation, debug setup, and GTK procedure loading; ignore later renderer resources.
  - path: src/apprt/gtk/class/surface.zig
    line: 3250
    end: 3380
    label: Current GTK GLArea lifecycle
    purpose: Read realize, unrealize, context-current, and render handoffs around the core renderer.
---
import EvolutionStrip from '../../../components/EvolutionStrip.astro';
import FidelityLegend from '../../../components/FidelityLegend.astro';
import LessonActions from '../../../components/LessonActions.astro';
import LocalLessonSetup from '../../../components/LocalLessonSetup.astro';
import OutputPreview from '../../../components/OutputPreview.astro';
import RectangleArtifact from '../../../components/RectangleArtifact.astro';
import SourceLink from '../../../components/SourceLink.vue';
import SyntaxBridge from '../../../components/SyntaxBridge.vue';

<LessonActions entryId="chapters/10-first-rectangle" />

<LocalLessonSetup chapter="10" />

## Let's make the blank window prove it can draw

<RectangleArtifact />

<OutputPreview file="src/data/reconstruction/chapter-10/output.txt" title="OpenGL capability and rectangle evidence" tag="chapter-10-first-rectangle" caption="The capture runs GTK's normal close path through an explicit capture-only timeout. CI repeats the native build, lifecycle smoke test, capture, and pixel checks." />

## Why draw a boring rectangle before text?

It is tempting to jump straight to letters. But one glyph would mix OpenGL setup, shaders, textures, font loading, rasterization, metrics, and blending. If the screen stayed blank, we would not know which part failed.

A rectangle lets us test one short path:

```text
GtkGLArea → current OpenGL context → viewport → known background → known region
```

Terminal state remains disconnected intentionally.

## First, make sure the window lifetime is trustworthy

Before touching OpenGL, we clean up a few weak spots in the GTK scaffold:

- runtime Surface now owns GtkWindow and GtkGLArea;
- normal execution waits for window close;
- capture timeout is opt-in;
- GTK callbacks live in a small C shim compiled against real headers;
- no-display initialization returns an error;
- a native CI job builds/tests GTK and runs Xvfb capture.

This work prevents the screenshot from hiding a broken lifecycle.

## Real Ghostty started with a simple GL clear too

<EvolutionStrip moments={[
 {kind:'then',eyebrow:'THEN · 2023-02-23',title:'Clear the first GLArea color',description:'Ghostty connected realize/render callbacks, loaded OpenGL, and cleared the GTK graphics surface to one color.',revision:'7f34afa',href:'https://github.com/ghostty-org/ghostty/blob/7f34afa3953289d33f95150c5f7d6a6d458a6993/src/apprt/gtk.zig#L180-L238',linkLabel:'Open first GTK render'},
 {kind:'reconstruction',eyebrow:'RECONSTRUCTION · CHAPTER 10',title:'Verify context and rectangle',description:'GtkGLArea requests OpenGL 4.3, records llvmpipe capabilities, clears a navy background, and scissor-clears a centered green region.',revision:'chapter-10-first-rectangle'},
 {kind:'now',eyebrow:'NOW · PINNED MAIN',title:'Drive the production renderer',description:'Current GTK Surface manages realize, unrealize, context errors, app-thread drawing, and renderer frame handoff.',revision:'6ad1fe7',href:'https://github.com/ghostty-org/ghostty/blob/6ad1fe7d8cbda36c77b337a96c9bea8a77883699/src/apprt/gtk/class/surface.zig#L3250-L3380',linkLabel:'Open current GLArea path'}
]} />

## Ask OpenGL what context we actually got

The realized GLArea reports:

```text
vendor=Mesa
renderer=llvmpipe
version=4.5 Core Profile
shading language=4.50
```

We ask for OpenGL 4.3 or newer. Here Mesa's `llvmpipe` software renderer gives us 4.5. That is good deterministic evidence, but it does not magically prove every hardware driver or Wayland setup.

## Follow the GTK render callback

<SyntaxBridge client:load reference="c-abi-boundaries" sourceLabel="C shim" title="Draw inside the current GL callback" code={[
  'static gboolean gl_render(GtkGLArea *area, GdkGLContext *context, gpointer data) {',
  '  gtk_gl_area_make_current(area);',
  '  int width = gtk_widget_get_width(GTK_WIDGET(area));',
  '  int height = gtk_widget_get_height(GTK_WIDGET(area));',
  '  glViewport(0, 0, width, height);',
  '  glScissor(width / 4, height / 4, width / 2, height / 2);',
  '  glClear(GL_COLOR_BUFFER_BIT);',
  '  return TRUE;',
  '}',
]} points={[
  { line:1, label:'native render callback', c:'GTK invokes this C ABI function with the GL area, current-context metadata, and the registered surface context pointer.', zig:'The Zig side deliberately calls a narrow shim instead of recreating GTK macros and callback ABI details.' },
  { line:2, label:'context currentness', c:'OpenGL commands affect thread-local current context state, so the area’s context must be made current before any GL call.', zig:'A pointer to a context is not enough; currentness is temporal state owned by the callback thread.' },
  { lines:[3,4,5], label:'coordinate space', c:'GTK reports logical widget dimensions; glViewport maps normalized device coordinates into framebuffer pixel coordinates from bottom-left.', zig:'The bridge must state which coordinate system crosses each boundary rather than treating width and height as universal.' },
  { lines:[6,7], label:'scissored clear', c:'The scissor rectangle limits a clear to the centered half-width, half-height pixel region.', zig:'This C shim owns GL command ordering; later Zig rendering can call a smaller typed boundary once the graphics contract is understood.' },
  { lines:[8,9], label:'callback lifetime', c:'TRUE reports that the frame was handled; GL resources and context remain GTK-owned beyond this stack frame.', zig:'Returning does not destroy the Surface, context, or window; teardown follows the registered native ownership path.' },
]} memory={[
  { owner:'GTK Surface', value:'window + GLArea widgets' },
  { owner:'render callback thread', value:'temporarily current GL context' },
  { owner:'OpenGL context', value:'viewport, scissor, clear state' },
  { owner:'framebuffer', value:'background + centered rectangle pixels' },
]} omission="TypeScript canvas and Python graphics wrappers can draw rectangles, but they hide GTK callback ABI, current-context state, OpenGL coordinates, and native resource lifetime—the exact concepts this chapter introduces." />

> **Text version:** GTK enters a C render callback with native pointers. The callback makes the GLArea context current, reads widget dimensions, establishes a bottom-left framebuffer viewport, restricts drawing with a centered scissor rectangle, and clears it. Returning handles the frame but does not free the GTK Surface or GL context; their native lifetime continues until teardown.

Read the callback like a short recipe:

1. makes the GLArea context current;
2. sets a viewport from widget dimensions;
3. clears the full client area to dark navy;
4. enables scissor testing;
5. clears the centered half-width/half-height region to green;
6. disables scissor testing.

Notice what we did not add: no shader, vertex buffer, texture, or renderer class. A clear plus scissor is enough to prove pixels can change.

## Check actual pixels, not just “the screenshot changed”

The capture checks three root-image samples:

```text
center     = srgb(51,204,102)
background = srgb(13,18,31)
root       = srgb(0,0,0)
```

The center must differ from the client background, and the client background must differ from the X root display. These three samples tell us the green rectangle, navy window, and black X root are all distinct. That is much stronger than saying “some screenshot bytes changed.”

## Fidelity

<FidelityLegend />

| File | Status | Deferred production behavior |
|---|---|---|
| `src/apprt/gtk.zig` | adapted | thin Zig ownership wrapper around the native shim |
| `src/apprt/gtk_shim.c` | adapted | GLArea lifecycle and one scissored clear; no renderer object, context loss, resize policy, or input |
| `build.zig` | adapted | optional GTK4/libepoxy and capture options |
| native CI | project-owned | deterministic llvmpipe evidence, not hardware/Wayland support |

## How does today's renderer grow from this callback?

<SourceLink client:load base={import.meta.env.BASE_URL} sourceCommit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699" path="src/renderer/OpenGL.zig" line={130} end={180} label="Current OpenGL initialization" note="Read version validation and GTK loading. Ignore resources not earned by the rectangle." />

<SourceLink client:load base={import.meta.env.BASE_URL} sourceCommit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699" path="src/apprt/gtk/class/surface.zig" line={3250} end={3380} label="Current GtkGLArea lifecycle" note="Read realize/unrealize/render ownership and context error handling." />

## Checkpoint

```text
implementation  5bc46b51742610097358128b5391c3471f491a72
tag             chapter-10-first-rectangle
```

## Geometry works—how do we draw one letter?

The graphics surface can draw geometry but not text. The next checkpoint should isolate font discovery, metrics, and one rasterized glyph before introducing an atlas or terminal-cell rendering.

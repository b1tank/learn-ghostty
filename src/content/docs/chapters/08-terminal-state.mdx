---
title: "Chapter 08: Terminal state and debug frame"
description: Apply parser actions to styled cells and cursor state, then render a deterministic textual frame.
order: 8
module: Reconstruction
status: published
duration: 45–60 minutes
sidebar:
  order: 8
next: false
historyRefs:
  - commit: dc788ce5b2ca24b3a97eb4d762dc0d3acde9e2ea
    date: "2022-04-17"
    path: src/terminal/Terminal.zig
    line: 1
    end: 44
    label: First Terminal state
    purpose: See the earliest terminal owner begin with allocator, grid, and parser before behavior accumulated.
    context: Ghostty introduced durable terminal state one day before its parser state table.
  - commit: fc8bd859db957ff87eb03682a26e0a1808dc1f49
    date: "2022-04-18"
    path: src/terminal/Terminal.zig
    line: 1
    end: 182
    label: First string and multiline updates
    purpose: See printable and control actions begin mutating rows and cursor state.
    context: The first useful Terminal behavior wrote strings and handled multiline movement.
  - commit: ccaf75193b22cd2bb486e4a6414a0c33696a209f
    date: "2022-05-12"
    path: src/terminal/Terminal.zig
    line: 1
    end: 729
    label: First connected SGR attributes
    purpose: See parser SGR attributes begin changing state used by printed cells.
    context: Style mutation was connected weeks after basic printable and cursor behavior.
sourceRefs:
  - path: src/terminal/Terminal.zig
    line: 300
    end: 340
    label: Current Terminal initialization
    purpose: Read the ownership inventory and initialized screen/cursor state; ignore configuration breadth.
  - path: src/terminal/Terminal.zig
    line: 1120
    end: 1160
    label: Current print path
    purpose: See how printable codepoints reach cells while honoring cursor and screen modes.
  - path: src/terminal/Terminal.zig
    line: 3573
    end: 3595
    label: Current style application
    purpose: See SGR attributes delegated into current style state.
---
import CodeSnapshot from '../../../components/CodeSnapshot.astro';
import EvolutionStrip from '../../../components/EvolutionStrip.astro';
import FidelityLegend from '../../../components/FidelityLegend.astro';
import FrontierMap from '../../../components/FrontierMap.astro';
import LessonActions from '../../../components/LessonActions.astro';
import OutputPreview from '../../../components/OutputPreview.astro';
import SourceLink from '../../../components/SourceLink.vue';
import TerminalGridDiagram from '../../../components/TerminalGridDiagram.astro';

<LessonActions entryId="chapters/08-terminal-state" />

## What you will see

Parser actions now survive as cells, styles, and cursor position:

<OutputPreview file="src/data/reconstruction/chapter-08/output.txt" title="A deterministic terminal frame without a GUI" tag="chapter-08-terminal-state" caption="Dots represent blank cells and G represents green cell style. The textual frame is generated from the same Terminal state a later renderer will consume." />

<TerminalGridDiagram />

## The limitation

Chapter 07 produced correct actions and then discarded them after tracing. A renderer cannot draw actions; it needs durable state representing the current terminal screen.

<FrontierMap current="Terminal state" />

The next boundary owns:

```text
cells + per-cell style + cursor + current style
```

It still does not own glyphs, textures, frames, or a native window.

## How Ghostty accumulated terminal state

Ghostty introduced `Terminal.zig` on April 17, 2022 with a grid and parser owner. The next day printable strings and multiline movement started working. SGR attributes were connected on May 12. The modern screen/page model accumulated through years of scrolling, Unicode, modes, alternate screens, selection, hyperlinks, images, and performance work.

<EvolutionStrip moments={[
  { kind:'then', eyebrow:'THEN · 2022-04-18', title:'Write strings into a grid', description:'Early Terminal state turned print and controls into rows and cursor movement before broad VT behavior existed.', revision:'fc8bd85', href:'https://github.com/ghostty-org/ghostty/blob/fc8bd859db957ff87eb03682a26e0a1808dc1f49/src/terminal/Terminal.zig', linkLabel:'Open early Terminal' },
  { kind:'reconstruction', eyebrow:'RECONSTRUCTION · CHAPTER 08', title:'Keep only earned state', description:'A fixed 3×24 grid stores ASCII cells, default/green style, and cursor state produced by the live child interaction.', revision:'chapter-08-terminal-state' },
  { kind:'now', eyebrow:'NOW · PINNED MAIN', title:'Model production terminal behavior', description:'Current Terminal manages paged screens, scrollback, Unicode, modes, styles, links, images, selection, damage, and many protocol responses.', revision:'6ad1fe7', href:'https://github.com/ghostty-org/ghostty/blob/6ad1fe7d8cbda36c77b337a96c9bea8a77883699/src/terminal/Terminal.zig', linkLabel:'Open current Terminal' },
]} />

The reconstruction does not copy modern page abstractions backward. Three fixed rows are enough to prove the parser-to-state contract.

## Step 1: define cells and active style

<CodeSnapshot file="src/data/reconstruction/chapter-08/terminal/Terminal.zig" label="src/terminal/Terminal.zig" status="adapted" reason="Fixed 3×24 ASCII cells, cursor, default/green style, action application, and textual frame; production pages, modes, Unicode, and scrollback are deferred." />

A cell stores what the renderer eventually needs from this checkpoint:

```zig
pub const Cell = struct {
    char: u8 = ' ',
    style: Style = .default,
};
```

Terminal also stores `cursor_row`, `cursor_column`, and the current style applied to future printed cells.

## Step 2: apply parser actions

The mapping is intentionally direct:

| Parser action | State transition |
|---|---|
| `print(byte)` | write cell with current style; advance cursor |
| `execute(CR)` | cursor column becomes zero |
| `execute(LF)` | cursor moves to next row |
| `sgr(32)` | current style becomes green |
| `sgr(0)` | current style becomes default |

The Terminal never sees ESC or CSI bytes. That syntax was consumed by Parser.

## Step 3: update state after every read chunk

Termio records the Parser action count before feeding bytes, then applies only newly emitted actions:

```zig
const action_start = parser.action_count;
try parser.feed(bytes);
terminal.apply(parser.actionsSince(action_start));
```

This prevents old actions from being replayed and makes state advance incrementally with the live stream.

<CodeSnapshot file="src/data/reconstruction/chapter-08/termio/Termio.zig" label="src/termio/Termio.zig" status="adapted" reason="Owns Parser and Terminal together, applying newly emitted actions synchronously after each PTY read chunk." />

## Step 4: inspect the resulting frame

The child interaction produces:

```text
ready>       from child
hello        PTY local echo
CR LF        move to row 1
SGR 32       future cells green
reply:hello  green cells
SGR 0        future cells default
CR LF        cursor to row 2, column 0
```

The first row therefore contains `ready>hello` in default style. The second contains `reply:hello` in green. The third is blank with the cursor at its start.

## Step 5: use a debug frame before pixels

`Terminal.trace` renders:

- blank characters as `.`;
- default styles as `.`;
- green styles as `G`;
- cursor and current style as metadata.

This is a real renderer boundary in the architectural sense: it consumes state and produces an observable representation. It is not yet a graphical renderer.

## Build and run

```console
zig fmt --check .
zig build
zig build test
zig build run
```

The Terminal unit test applies print, SGR, CR, and LF actions and asserts exact cells, style, and cursor movement independently of PTY behavior.

## Fidelity

<FidelityLegend />

| File | Status | Deferred production behavior |
|---|---|---|
| `src/terminal/Terminal.zig` | adapted | dynamic pages, scrollback, Unicode, wide cells, modes, alternate screen, tabs, selection, hyperlinks, images, damage |
| `src/terminal/Parser.zig` | adapted | exposes action slices for incremental state application |
| `src/termio/Termio.zig` | adapted | owns synchronous Terminal; no renderer snapshot or mailbox |
| `src/Surface.zig` | adapted | emits textual frame rather than native rendering |

## Compare with current Ghostty

<SourceLink client:load base={import.meta.env.BASE_URL} sourceCommit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699" path="src/terminal/Terminal.zig" line={300} end={340} label="Current Terminal initialization" note="Read the durable state owners and initialization order. Ignore configuration values not yet reconstructed." />

<SourceLink client:load base={import.meta.env.BASE_URL} sourceCommit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699" path="src/terminal/Terminal.zig" line={1120} end={1160} label="Current print path" note="Read how a printable codepoint reaches screen cells and cursor logic. Ignore unsupported modes for now." />

<SourceLink client:load base={import.meta.env.BASE_URL} sourceCommit="6ad1fe7d8cbda36c77b337a96c9bea8a77883699" path="src/terminal/Terminal.zig" line={3573} end={3595} label="Current SGR style application" note="Connect parser attributes to current style state used by future cells." />

## Checkpoint

```text
commit  d730e085c760ddaadf654c5c4430f3099932d3c9
tag     chapter-08-terminal-state
```

## The next limitation

The debug frame proves state, but users still cannot see a native application surface. The next chapter must choose and initialize the first real platform runtime, open a blank window, and capture an actual screenshot before any GPU or font complexity is added.
